<%= form_tag(search_page_path, :method => "get",
    class: 'navbar-form navbar-left') do |s| %>


  <div class="input-group">


    <%= search_field_tag :search, params[:search], placeholder: "Search", class: "form-control" %>
     <div class="dropdown">
      <select class="fa fa-caret-down">

    <div class="dropdown-content">
      <% @categories.each do |category|%>
     <%# <%= select_tag :id, options_for_select(@categories.name) %>
      <option> <%= link_to category.name %> </option>
      <% end %>
    </div>
    </select>
  </div>
    <div class="input-group-btn">
      <%= button_tag "", :class => 'btn btn-info glyphicon glyphicon-search',:name => nil%>
    </div>
  </div>
<% end %>
<ul class="product-display" >
<%= render partial: 'category/category', collection: @categories %>
</ul>